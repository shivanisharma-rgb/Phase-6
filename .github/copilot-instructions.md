# Student Feedback Manager

- Use JavaScript with CommonJS modules.
- Keep the Express API and static frontend in the project root.
- Keep database credentials in `.env`, never in committed source files.
- Validate API changes with `npm start` and the `/api/health` endpoint.
